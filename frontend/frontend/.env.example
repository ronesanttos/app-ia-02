# URL do Django (sem barra no final)
VITE_API_BASE_URL=http://127.0.0.1:8000

# Se definir API_KEY no backend/.env, OBRIGATÓRIO repetir aqui (senão o botão ML retorna 403).
# VITE_API_KEY=sua-chave-aqui
